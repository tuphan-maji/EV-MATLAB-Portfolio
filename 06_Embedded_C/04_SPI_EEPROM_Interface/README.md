SPI EEPROM Interface Using Embedded C
Objective

The objective of this project is to develop an Embedded C program that interfaces with an external EEPROM using the SPI (Serial Peripheral Interface) protocol. The program performs both write and read operations on EEPROM memory and verifies the stored data. This project demonstrates the basic principle used in embedded systems, Battery Management Systems (BMS), Vehicle Control Units (VCU), and automotive Electronic Control Units (ECUs) for non-volatile data storage and retrieval.

Theory

The Serial Peripheral Interface (SPI) is a synchronous serial communication protocol widely used for communication between a microcontroller and peripheral devices such as EEPROMs, sensors, ADCs, DACs, displays, and communication modules.

SPI communication uses four signals:

MOSI (Master Out Slave In) – Data transmitted from the microcontroller to the EEPROM.

MISO (Master In Slave Out) – Data transmitted from the EEPROM to the microcontroller.

SCK (Serial Clock) – Clock signal generated by the microcontroller.

CS (Chip Select) – Used to select or deselect the EEPROM device.

An SPI EEPROM stores data in non-volatile memory, which means the stored information remains available even after power is removed. The microcontroller communicates with the EEPROM by sending command bytes followed by address bytes and data bytes.

In this project, the EEPROM communication sequence is:

Send Write Enable (0x06)

Send Write Command (0x02)

Send the memory address

Send the data byte to be stored

Send Read Command (0x03)

Read back the stored data

This approach is commonly used in automotive embedded systems because it provides reliable and fast communication with external memory devices.

Mathematical Representation
EEPROM Write Sequence

Operation                                  Value       
Write Enable Command                        0x06

Write Command                               0x02

Address                                     0x0010

Data                                        0x55

EEPROM Read Sequence

Operation                                   Value

Read Command                                0x03

Address                                     0x0010

Read Data                                   EEPROM[0x0010]

Input Parameters

Parameter                                   Value

EEPROM Command                              0x06

Write Command                               0x02

Read Command                                0x03

Address High Byte                           0x00

Address Low Byte                            0x10

Data to Write                               0x55

Embedded C File

SPI_EEPROM_Interface.c

Main source file containing SPI EEPROM write and read functions

Program Flow

Start

Initialize EEPROM Memory

CS LOW

Send Write Enable (0x06)

CS HIGH

CS LOW

Send Write Command (0x02)

Send Address (0x0010)

Send Data (0x55)

CS HIGH

Store Data in EEPROM

CS LOW

Send Read Command (0x03)

Send Address (0x0010)

Read Data from EEPROM

CS HIGH

Display Read Data

End

Output

The program performs EEPROM write and read operations and verifies that the stored value is read back correctly.

Sample Output

SPI EEPROM Practical Example

CS LOW

SPI Transfer : 0x06

CS HIGH

CS LOW

SPI Transfer : 0x02

SPI Transfer : 0x00

SPI Transfer : 0x10

SPI Transfer : 0x55

CS HIGH

Write Completed.

CS LOW

SPI Transfer : 0x03

SPI Transfer : 0x00

SPI Transfer : 0x10

CS HIGH

Data Read = 0x55

Applications

This SPI EEPROM interface technique is used in:

Battery Management Systems (BMS)

Vehicle Control Units (VCU)

EV parameter storage

Calibration data storage

Fault log storage

Automotive diagnostics

Industrial embedded systems

Data logging systems

Future Improvements

The project can be extended with additional embedded features such as:

Status register checking to verify write completion.

Page write operation for multiple-byte storage.

Sequential read operation.

CRC or checksum verification.

Interrupt-based SPI communication.

Real-time EEPROM data logging.

Integration with STM32, PIC, or AVR microcontrollers.

UART debugging output for embedded hardware testing.

Conclusion

This project successfully demonstrates SPI-based EEPROM interfacing using Embedded C. The microcontroller sends a Write Enable command, writes the data byte 0x55 to EEPROM address 0x0010, and reads the same value back to verify correct operation. The project provides a clear understanding of SPI communication, external memory interfacing, and non-volatile data storage techniques used in EV and automotive embedded systems.

Author

Tuphan Maji

